{
  "name": "ai-doc-workspace-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev --prefix workspace -- --port 5173\" \"tsx watch server/index.ts\"",
    "build": "npm run build --prefix workspace && tsc -p tsconfig.server.json",
    "start": "NODE_ENV=production node dist-server/index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.57.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.1",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  }
}
