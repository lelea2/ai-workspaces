# Copy to .env and set the provider.
# 'mock' — deterministic mock responses, no server needed (default for dev)
# 'proxy' — calls Express server at /api/ai/*, requires OPENAI_API_KEY in root .env

VITE_AI_PROVIDER=mock
